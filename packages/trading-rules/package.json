{
  "name": "@topgun/trading-rules",
  "version": "0.3.5-phase3.5",
  "private": true,
  "description": "TopGun Trading detectors and indicator helpers. Pure functions, no IO. Phase 3.5 activates this package with detectLargeTrades for the Whale Activity observational feature.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@topgun/types": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@topgun/config": "workspace:*",
    "@types/node": "^20.14.15",
    "eslint": "^9.9.0",
    "typescript": "^5.5.4",
    "vitest": "^1.6.0"
  }
}
