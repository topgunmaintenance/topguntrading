# @topgun/web

> **Phase 1 scaffold.** This package is intentionally empty. Real Next.js
> wiring lands in Phase 2 — see `docs/roadmap.md`.

The TopGun Trading web app. Marketing site, authenticated app shell,
chart workspace, journal, replay, and AI surfaces all live here.

## Phase 2 plan

- Next.js 14+ App Router
- TypeScript strict
- Tailwind CSS + shadcn/ui from `@topgun/ui`
- Auth via `@topgun/api` session bridge
- Shared types from `@topgun/types`

## Owner

Frontend Lead — see `agents/frontend-lead.md`.
