# @topgun/api

> **Phase 1 scaffold.** This package is intentionally empty. Real
> NestJS wiring lands in Phase 2 — see `docs/roadmap.md`.

The TopGun Trading API. REST endpoints, WebSocket gateways, auth,
market data fan-out, AI gateway, and the source of truth for shared
schemas.

## Phase 2 plan

- NestJS 10+
- Prisma + PostgreSQL
- Redis (cache, pubsub, BullMQ producer)
- WebSocket gateway
- Auth: sessions + JWT + refresh, OAuth bridges
- Inputs validated with zod from `@topgun/types`

## Owner

Backend Lead — see `agents/backend-lead.md`.
